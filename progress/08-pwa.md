# Task 8 — PWA wrapper (manifest, service worker, icons)

**Status:** ✅ done · **Issue:** #8 · **Merged:** PR #19

## Done
- `public/manifest.webmanifest` — name "AI English Mentor", short_name, display `standalone`, theme/background color, `start_url "."`, icons 192 + 512 (incl. `maskable`).
- `public/sw.js` — precaches the app shell (index.html, styles.css, all JS modules, manifest, icons); cache-first; offline navigation falls back to the cached index; **skips `/api/*` and non-GET** so the proxy always hits the network.
- `index.html` — `<link rel="manifest">` + apple-touch-icon (theme-color meta already present).
- `app.js` — registers `sw.js` (guarded by `'serviceWorker' in navigator`).
- `public/icons/icon-192.png` + `icon-512.png` — generated by `scripts/gen-icons.mjs` (tiny hand-rolled PNG encoder, no deps; re-runnable).
- Tests: E2E `pwa.spec.js` (3): manifest linked + fetchable + valid; SW registers/active; app shell loads offline after first visit. **48 unit/API green** locally.

## Outcome
✅ **Done.** Merged via PR #19; CI green (incl. the SW/offline E2E). The app is an installable PWA with an offline shell. Next: **Issue #9** (README + hosting + Android install docs) — the final task.

## Notes
- Real "Add to Home Screen" install + Lighthouse PWA audit are owner-side on Android Chrome.
- Express (`server/app.js`) already serves `public/`, so the manifest/SW/icons are served in prod too.
